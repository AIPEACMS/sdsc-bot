name: sdsc_bot
description: SDSC Telegram bot for volunteer availability and allocation.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  televerse: ^3.6.0
  sqlite3: ^3.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
